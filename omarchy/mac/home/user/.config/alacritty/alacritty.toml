general.import = [ "~/.local/state/omarchy/current/theme/alacritty.toml" ]

[env]
TERM = "xterm-256color"

[font]
normal = { family = "PxPlus IBM VGA 8x16", style = "Regular" }
bold = { family = "PxPlus IBM VGA 8x16", style = "Regular" }
italic = { family = "PxPlus IBM VGA 8x16", style = "Regular" }
offset = { x = 0, y = -2 } # Adjust 'y' for line spacing
#normal = { family = "CaskaydiaMono Nerd Font", style = "Regular" }
#bold = { family = "CaskaydiaMono Nerd Font", style = "Bold" }
#italic = { family = "CaskaydiaMono Nerd Font", style = "Italic" }
size = 17

[window]
padding.x = 14
padding.y = 14
decorations = "None"
opacity = 1

[cursor]
style = { shape = "Underline", blinking = "Always" }
blink_interval = 750 # Adjust as needed, in milliseconds
blink_timeout = 0 # 0 disables timeout, making blinking persistent

[keyboard]
bindings = [
    # Disable Ctrl+L clearing the screen
    { key = "L", mods = "Control", action = "None" }
    #{ key = "Insert", mods = "Shift", action = "Paste" },
    #{ key = "Insert", mods = "Control", action = "Copy" },
    # Send Shift+Return as CSI-u so TUIs can distinguish it from Return without treating it as Alt+Return.
    #{ key = "Return", mods = "Shift", chars = "\u001B[13;2u" },
    # Legacy encoding sends Alt+Shift+Return the same as Alt+Return; send CSI-u so tmux can match M-S-Enter.
    #{ key = "Return", mods = "Alt|Shift", chars = "\u001B[13;4u" }
]
