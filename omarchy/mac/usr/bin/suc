#!/bin/sh

class=$(hyprctl activewindow -j | jq -r .class)

if [ "$class" = "Alacritty" ]; then
    hyprctl dispatch sendshortcut "CTRL SHIFT, C, active"
else
    hyprctl dispatch sendshortcut "CTRL, C, active"
fi
